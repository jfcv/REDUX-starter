/**
 * simplest bug's structure state
 */
const state = [{ id: 1, description: "", resolved: false }];

/**
 * with users included
 */
const advancedState = {
  bugs: [{ id: 1, description: "", resolved: false }],
  currentUser: {},
};
